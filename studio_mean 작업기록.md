# studio_mean 작업기록

## 2026-07-29 온라인 배포 전환

- 기존 글쓰기 원칙과 정신건강 블로그 초안-검수 2단계를 유지했습니다.
- OpenAI 키를 브라우저가 아닌 로컬 서버 및 배포 서버의 비밀 환경 변수에서만 읽도록 구성했습니다.
- Cloudflare Worker 호환 온라인 API와 Sites 배포 빌드를 추가했습니다.
- 사용되지 않던 쿠팡, 퀴즈, 스토리텔링, TTS, GitHub 브라우저 설정 및 중복 루트 파일을 제거했습니다.
- OpenAI 연결, 전체 글 생성 규칙, 빌드, 비밀 파일 차단을 검증했습니다.
- 운영 주소는 `https://studio-mean-writing.meanismmm.chatgpt.site`이며, 기존 GitHub Pages 주소는 운영 주소로 자동 이동하도록 처리했습니다.
- GitHub Pages의 `/studio_mean/` 경로를 새 운영 주소에 그대로 붙여 404가 발생한 문제를 수정하고, 새 운영판 루트로 이동하도록 확정했습니다.
- 사용자 확인 결과 `10대 청소년·공황장애`에서 추천된 "병원에서는 무엇을 할까요?" 제목과 본문이 불일치했습니다. 원인은 연령·독자 조건이 원고 단계에 전달되지 않은 점, 제목과 무관한 고정 문단 역할, 검수 단계의 원제목 누락이었습니다.
- 주제 추천 결과에 핵심 질문·직접 답변·6개 문단 계획을 포함하고, 연령·독자 조건을 초안과 최종 검수까지 전달하도록 수정했습니다. 치료 주제에서 일반적인 임상 평가와 치료 설명까지 금지하던 충돌 지침도 행정 정보 금지와 임상 설명 허용으로 분리했습니다.
- 동일한 제목으로 실제 OpenAI 초안·고강도 검수 시험을 수행했습니다. 2,137자, 평가·인지행동치료·약물치료 포함, 청소년 대상 유지, 제목·마커·서명·분량 검사를 모두 통과했습니다.

## 2026-07-29

### 요청 의도

- Claude 구독 종료 이후 저하된 블로그 원고 품질을 개선하기 위해 OpenAI API 기반으로 전환합니다.
- 기존 정신건강의학과 블로그의 문단 구조, 톤앤매너, 금지 표현, 의학적 정확성 원칙은 유지합니다.
- OpenAI API 키는 브라우저에 노출하지 않고 볼트 루트 `.env`에서 사용합니다.

### 확정 사항

| 항목 | 결정 |
|---|---|
| 모델 | `gpt-5.6-sol` |
| API | OpenAI Responses API |
| 추론 수준 | 주제 추천 `low`, 원고 작성·검수 `medium` |
| 원고 흐름 | 주제 추천 → 초안 → 편집 검수 → 결과 표시 |
| API 키 | 볼트 루트 `.env`의 `OPENAI_API_KEY`, 브라우저 전달 금지 |
| 실행 방식 | `server.js` 로컬 서버, `127.0.0.1:8787` |

### 주요 변경

- Anthropic 직접 호출을 제거하고 `/api/generate` 로컬 백엔드 호출로 전환했습니다.
- `PSYCH_SYSTEM` 작성 원칙을 유지하고 `PSYCH_REVIEW_SYSTEM` 최종 검수 단계를 추가했습니다.
- 설정 화면에서 Claude/Gemini 키 입력을 제거하고 OpenAI 서버 연결 상태를 표시합니다.
- `.env`, `.git` 등 숨김 경로의 정적 접근을 차단했습니다.
- `.gitignore`, `.env.example`, 실행 문서, 통합 테스트를 추가했습니다.
- 활성 코드와 중복된 루트 JS/CSS 파일을 제거했습니다.

### 검증 결과

| 검증 | 결과 |
|---|---|
| JavaScript 구문 검사 | 통과 |
| 로컬 서버 상태·정적 페이지 | 통과 |
| OpenAI 실제 연결 | `gpt-5.6-sol` 응답 확인 |
| 실전 원고 초안·검수 | 통과 |
| 최종 원고 분량 | 2,035자 |
| 제목 브랜드 문구 | 통과 |
| 구분선 3개 | 통과 |
| 지도·사진 마커 | 통과 |
| 서명 | 통과 |
| 마크다운 제목 금지 | 통과 |

### 관련 파일

- [[README]]
- [[index]]
- [[js/blog]]
- [[js/api]]
- [[server]]
